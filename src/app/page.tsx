import KioskHomeClient from '@/src/components/KioskHomeClient';

export default function KioskPage() {
  return (
    <KioskHomeClient
      initialPurposes={[]}
      initialBadges={[]}
      initialUsedBadgeNumbers={[]}
    />
  );
}
